{
  "hash": "7e6c58c8a23129d48844447bb9b9eeea",
  "result": {
    "engine": "knitr",
    "markdown": "# Análisis de componentes principales\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\nlibrary(knitr)\n```\n:::\n\n\n## Motivación: los datos no llenan todo el espacio\n\nImaginemos imágenes de dígitos escritos a mano, como las del conjunto MNIST. Si cada imagen tiene $28 \\times 28$ píxeles, cada dígito puede representarse como un punto con 784 variables. En principio, el espacio posible es enorme: cualquier combinación de intensidades de píxeles sería un punto válido.\n\nPero los dígitos reales no ocupan uniformemente ese espacio. La mayoría de combinaciones posibles de píxeles no parecen números. Los dígitos viven cerca de estructuras de dimensión mucho menor: formas con trazos, bucles, inclinaciones, anchuras y curvaturas. Reducir dimensión consiste en encontrar características más informativas que las variables originales.\n\nPCA hace una versión lineal de esta idea. Construye un nuevo sistema de coordenadas que concentra la mayor cantidad posible de variabilidad en los primeros ejes. No descubre conceptos como «bucle» de forma semántica, pero sí combinaciones de píxeles que recogen patrones dominantes de variación.\n\nAntes de trabajar con cientos de variables, podemos entender casi todo el mecanismo observando qué ocurre al proyectar una nube bidimensional sobre una recta.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(123)\nn <- 120\nz1 <- rnorm(n, sd = 2.5)\nz2 <- rnorm(n, sd = 0.35)\ntheta <- pi / 5\nR <- matrix(c(cos(theta), -sin(theta), sin(theta), cos(theta)), nrow = 2)\nX_sim <- cbind(z1, z2) %*% t(R)\ncolnames(X_sim) <- c(\"x1\", \"x2\")\n\npca_sim <- prcomp(X_sim, center = TRUE, scale. = FALSE)\nejes_sim <- tibble(\n  componente = c(\"PC1\", \"PC2\"),\n  x = pca_sim$rotation[1, ] * c(4, 2),\n  y = pca_sim$rotation[2, ] * c(4, 2)\n)\n\nas_tibble(X_sim) |>\n  ggplot(aes(x1, x2)) +\n  geom_point(color = \"gray40\", alpha = 0.8) +\n  geom_segment(\n    data = ejes_sim,\n    aes(x = 0, y = 0, xend = x, yend = y, color = componente),\n    arrow = grid::arrow(length = grid::unit(0.18, \"cm\")),\n    linewidth = 1.1,\n    inherit.aes = FALSE\n  ) +\n  coord_equal() +\n  labs(\n    title = \"Una nube alargada no llena el plano\",\n    x = \"x1\", y = \"x2\", color = \"Componente\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/simulacion-geometrica-1.png){width=672}\n:::\n:::\n\n\nLa primera componente apunta en la dirección donde la nube se extiende más. La segunda es perpendicular y recoge la variabilidad restante.\n\n::: {.callout-note}\nPCA conserva direcciones de alta variabilidad. Eso no garantiza que sean las más relevantes para cualquier pregunta aplicada.\n:::\n\n## Geometría de una proyección\n\nSupongamos que los datos están centrados y representamos el individuo $i$ mediante el vector columna $x_i$. Elegimos una dirección unitaria\n\n$$\na =\n\\begin{pmatrix}\na_1\\\\\n\\vdots\\\\\na_p\n\\end{pmatrix},\n\\qquad a^\\top a=1.\n$$\n\nSu coordenada al proyectarlo sobre esa dirección es el escalar\n\n$$\nz_i=a^\\top x_i.\n$$\n\nPara volver al espacio original usando solo esa coordenada, reconstruimos\n\n$$\n\\hat x_i=z_i a=aa^\\top x_i.\n$$\n\nAsí, $z_i$ indica dónde cae el individuo sobre el nuevo eje, $\\hat x_i$ es el punto proyectado y $x_i-\\hat x_i$ es la parte que se pierde.\n\n\n::: {.cell}\n\n```{.r .cell-code}\na_pc1 <- pca_sim$rotation[, 1]\nz_pc1 <- drop(X_sim %*% a_pc1)\nX_proy_pc1 <- tcrossprod(z_pc1, a_pc1)\nindices_muestra <- unique(round(seq(4, n, length.out = 14)))\n\nsegmentos_proyeccion <- tibble(\n  x1 = X_sim[indices_muestra, 1],\n  x2 = X_sim[indices_muestra, 2],\n  x1_proy = X_proy_pc1[indices_muestra, 1],\n  x2_proy = X_proy_pc1[indices_muestra, 2]\n)\n\nas_tibble(X_sim) |>\n  ggplot(aes(x1, x2)) +\n  geom_abline(\n    slope = a_pc1[2] / a_pc1[1], intercept = 0,\n    color = \"#2c7fb8\", linewidth = 0.9\n  ) +\n  geom_segment(\n    data = segmentos_proyeccion,\n    aes(xend = x1_proy, yend = x2_proy),\n    linetype = \"dashed\", color = \"gray45\"\n  ) +\n  geom_point(color = \"gray55\", alpha = 0.45) +\n  geom_point(\n    data = segmentos_proyeccion,\n    aes(x = x1_proy, y = x2_proy),\n    color = \"#d95f0e\", size = 2\n  ) +\n  coord_equal() +\n  labs(\n    title = \"Proyección ortogonal sobre una dirección\",\n    subtitle = \"Los puntos naranjas son las reconstrucciones sobre la recta\",\n    x = \"x1\", y = \"x2\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/proyeccion-recta-1.png){width=672}\n:::\n:::\n\n\n### Una dirección buena y una mala\n\nPCA no maximiza la longitud de la flecha que dibujamos, sino la dispersión de las coordenadas $z_i$ después de proyectar los individuos. Comparemos PC1 con la dirección perpendicular, PC2.\n\n\n::: {.cell}\n\n```{.r .cell-code}\npreparar_proyeccion <- function(a, direccion) {\n  z <- drop(X_sim %*% a)\n  reconstruccion <- tcrossprod(z, a)\n  error <- sum((X_sim - reconstruccion)^2)\n\n  tibble(\n    x1 = X_sim[, 1],\n    x2 = X_sim[, 2],\n    x1_proy = reconstruccion[, 1],\n    x2_proy = reconstruccion[, 2],\n    direccion = paste0(\n      direccion,\n      \"\\nVarianza = \", round(var(z), 2),\n      \"; error = \", round(error, 1)\n    )\n  )\n}\n\ncomparacion_proyecciones <- bind_rows(\n  preparar_proyeccion(pca_sim$rotation[, 1], \"Dirección PC1\"),\n  preparar_proyeccion(pca_sim$rotation[, 2], \"Dirección perpendicular\")\n)\n\nrectas_proyeccion <- tibble(\n  direccion = unique(comparacion_proyecciones$direccion),\n  pendiente = pca_sim$rotation[2, ] / pca_sim$rotation[1, ]\n)\n\ncomparacion_proyecciones |>\n  ggplot(aes(x1, x2)) +\n  geom_abline(\n    data = rectas_proyeccion,\n    aes(slope = pendiente, intercept = 0),\n    color = \"#2c7fb8\", linewidth = 0.8\n  ) +\n  geom_segment(\n    aes(xend = x1_proy, yend = x2_proy),\n    color = \"gray55\", alpha = 0.28\n  ) +\n  geom_point(color = \"gray25\", alpha = 0.65, size = 1.2) +\n  facet_wrap(vars(direccion)) +\n  coord_equal() +\n  labs(\n    title = \"Más varianza proyectada, menos error de reconstrucción\",\n    x = \"x1\", y = \"x2\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/buena-mala-proyeccion-1.png){width=672}\n:::\n:::\n\n\nEn PC1 las proyecciones quedan muy dispersas y los segmentos punto--proyección son globalmente cortos. En la dirección perpendicular ocurre lo contrario. Las distribuciones de los scores hacen visible la diferencia de dispersión.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntibble(\n  `PC1: dirección de máxima varianza` = drop(X_sim %*% pca_sim$rotation[, 1]),\n  `PC2: dirección perpendicular` = drop(X_sim %*% pca_sim$rotation[, 2])\n) |>\n  pivot_longer(everything(), names_to = \"direccion\", values_to = \"score\") |>\n  ggplot(aes(score, fill = direccion)) +\n  geom_density(alpha = 0.55, show.legend = FALSE) +\n  facet_wrap(vars(direccion), scales = \"free_y\") +\n  labs(title = \"Distribución de las coordenadas proyectadas\", x = \"Score\", y = \"Densidad\") +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/distribucion-proyecciones-1.png){width=672}\n:::\n:::\n\n\n### Máxima varianza y mínima pérdida son la misma idea\n\nLa proyección y el residuo son ortogonales. Por el teorema de Pitágoras,\n\n$$\n\\|x_i\\|^2=\\|\\hat x_i\\|^2+\\|x_i-\\hat x_i\\|^2.\n$$\n\nAl sumar sobre todos los individuos,\n\n$$\n\\sum_i\\|x_i\\|^2\n=\n\\sum_i\\|\\hat x_i\\|^2\n+\n\\sum_i\\|x_i-\\hat x_i\\|^2.\n$$\n\nLa cantidad de la izquierda es fija. Además, como $a$ tiene norma uno, $\\|\\hat x_i\\|^2=z_i^2$. Por tanto, maximizar la varianza de los scores equivale a minimizar la suma de errores cuadrados de reconstrucción.\n\nOtra forma de ver el problema es recorrer todas las direcciones del plano. Si\n\n$$\na(\\theta)=\n\\begin{pmatrix}\n\\cos\\theta\\\\\n\\sin\\theta\n\\end{pmatrix},\n$$\n\nla función que PCA maximiza es $V(\\theta)=a(\\theta)^\\top Sa(\\theta)$.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nS_sim <- cov(X_sim)\nangulos <- seq(0, pi, length.out = 361)\ndirecciones <- cbind(cos(angulos), sin(angulos))\nvarianzas_angulo <- apply(\n  direcciones,\n  1,\n  function(a) drop(t(a) %*% S_sim %*% a)\n)\nangulo_maximo <- angulos[which.max(varianzas_angulo)]\n\ntibble(angulo = angulos, varianza = varianzas_angulo) |>\n  ggplot(aes(angulo, varianza)) +\n  geom_line(color = \"#2c7fb8\", linewidth = 1) +\n  geom_vline(xintercept = angulo_maximo, linetype = \"dashed\", color = \"#d95f0e\") +\n  annotate(\n    \"point\", x = angulo_maximo, y = max(varianzas_angulo),\n    color = \"#d95f0e\", size = 2.5\n  ) +\n  scale_x_continuous(\n    breaks = c(0, pi / 4, pi / 2, 3 * pi / 4, pi),\n    labels = c(\"0\", expression(pi / 4), expression(pi / 2), expression(3 * pi / 4), expression(pi))\n  ) +\n  labs(\n    title = \"Varianza de la proyección según la dirección\",\n    subtitle = \"El máximo coincide con PC1, salvo el signo arbitrario del eje\",\n    x = expression(theta), y = \"Varianza proyectada\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/varianza-angulo-1.png){width=672}\n:::\n:::\n\n\n## Centrar, escalar y cambiar de base\n\nEstas tres operaciones cumplen funciones distintas.\n\n**Centrar** una variable,\n\n$$\nX_j^{(c)}=X_j-\\bar X_j,\n$$\n\ntraslada el origen al centro de la nube. No cambia las distancias entre individuos, la forma de la nube ni las correlaciones.\n\n**Estandarizar**,\n\n$$\nX_j^{(e)}=\\frac{X_j-\\bar X_j}{s_j},\n$$\n\ntambién modifica la escala de cada eje. Desde el punto de vista geométrico no es una mera traslación: altera las distancias y qué direcciones se consideran más variables. Por eso puede cambiar las componentes principales.\n\n**Cambiar de base** es lo que hace PCA una vez elegida la geometría de trabajo: construye ejes ortogonales nuevos. En resumen,\n\n$$\n\\text{centrar}\\neq\\text{escalar}\\neq\\text{cambiar de base}.\n$$\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndatos_originales <- as_tibble(X_sim) |>\n  transmute(x1 = x1 + 2.5, x2 = 1.6 * x2 - 2)\ndatos_centrados <- datos_originales |>\n  mutate(across(everything(), ~ .x - mean(.x)))\ndatos_estandarizados <- datos_originales |>\n  mutate(across(everything(), ~ as.numeric(scale(.x))))\npca_transformacion <- prcomp(datos_estandarizados, center = FALSE, scale. = FALSE)\n\npaneles_transformacion <- bind_rows(\n  datos_originales |> mutate(etapa = \"1. Datos originales\"),\n  datos_centrados |> mutate(etapa = \"2. Datos centrados\"),\n  datos_estandarizados |> mutate(etapa = \"3. Datos estandarizados\"),\n  datos_estandarizados |> mutate(etapa = \"4. Ejes PCA tras estandarizar\")\n)\n\nejes_transformacion <- tibble(\n  x = 0, y = 0,\n  xend = pca_transformacion$rotation[1, ] * pca_transformacion$sdev * 2,\n  yend = pca_transformacion$rotation[2, ] * pca_transformacion$sdev * 2,\n  etapa = \"4. Ejes PCA tras estandarizar\",\n  componente = c(\"PC1\", \"PC2\")\n)\n\npaneles_transformacion |>\n  ggplot(aes(x1, x2)) +\n  geom_point(color = \"gray40\", alpha = 0.65, size = 1.2) +\n  geom_segment(\n    data = ejes_transformacion,\n    aes(x = x, y = y, xend = xend, yend = yend, color = componente),\n    arrow = grid::arrow(length = grid::unit(0.13, \"cm\")),\n    linewidth = 0.9,\n    inherit.aes = FALSE\n  ) +\n  facet_wrap(vars(etapa), ncol = 2) +\n  coord_equal() +\n  labs(title = \"Centrar, escalar y construir ejes nuevos\", x = \"x1\", y = \"x2\", color = NULL) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/centrar-escalar-rotar-1.png){width=672}\n:::\n:::\n\n\n## Qué optimiza PCA\n\nSea $X$ la matriz de datos centrada, con los individuos en las filas, y\n\n$$\nS=\\frac{1}{n-1}X^\\top X\n$$\n\nsu matriz de covarianzas. Para una dirección unitaria $a$, los scores son $Xa$ y su varianza es\n\n$$\n\\operatorname{Var}(Xa)=a^\\top Sa.\n$$\n\nLa primera componente resuelve\n\n$$\n\\max_a a^\\top Sa\n\\qquad\\text{sujeto a}\\qquad\na^\\top a=1.\n$$\n\nLa restricción impide aumentar artificialmente la varianza multiplicando los coeficientes por una constante.\n\n### Por qué aparecen autovectores\n\nUsamos el multiplicador de Lagrange\n\n$$\n\\mathcal L(a,\\lambda)\n=a^\\top Sa-\\lambda(a^\\top a-1).\n$$\n\nAl derivar respecto de $a$,\n\n$$\n\\nabla_a\\mathcal L=2Sa-2\\lambda a=0,\n$$\n\ny, por tanto,\n\n$$\n\\boxed{Sa=\\lambda a}.\n$$\n\nLas direcciones candidatas son los autovectores de $S$. Si multiplicamos por $a^\\top$ y usamos $a^\\top a=1$,\n\n$$\na^\\top Sa=\\lambda a^\\top a=\\lambda.\n$$\n\nAsí,\n\n$$\n\\boxed{\\operatorname{Var}(PC_k)=\\lambda_k}.\n$$\n\nEl autovector determina la dirección y su autovalor mide la varianza capturada. PC1 usa el autovector asociado al mayor autovalor.\n\n### Componentes posteriores\n\nLa segunda componente resuelve el mismo problema añadiendo ortogonalidad respecto de la primera:\n\n$$\n\\max_a a^\\top Sa\n\\quad\\text{sujeto a}\\quad\na^\\top a=1,\n\\qquad a^\\top a_1=0.\n$$\n\nEl resultado es el autovector del segundo mayor autovalor, y así sucesivamente. Si reunimos los autovectores como columnas de $A$,\n\n$$\nS=A\\Lambda A^\\top,\n\\qquad A^\\top A=I,\n$$\n\ny definimos los scores $Z=XA$, entonces\n\n$$\n\\operatorname{Cov}(Z)=A^\\top SA=\\Lambda.\n$$\n\nEsta igualdad resume tres hechos: las componentes están incorreladas, la varianza de PC$_k$ es $\\lambda_k$ y la covarianza queda diagonal en el nuevo sistema.\n\nCuando hay autovalores iguales, las direcciones individuales dentro de su subespacio pueden no ser únicas. El subespacio representado sí es el mismo.\n\n### Varianza total y traza\n\nLa varianza total es la suma de las varianzas originales:\n\n$$\n\\operatorname{tr}(S)=\\lambda_1+\\cdots+\\lambda_p.\n$$\n\nPCA no crea varianza: la redistribuye entre ejes nuevos, ordenados de mayor a menor. La proporción explicada por PC$_k$ es\n\n$$\n\\frac{\\lambda_k}{\\sum_{m=1}^p\\lambda_m}.\n$$\n\n## SVD y `prcomp()`\n\nEl PCA también puede obtenerse sin formar explícitamente $S$. Para una matriz centrada,\n\n$$\nX=UDV^\\top.\n$$\n\nEntonces,\n\n$$\nS=\\frac{1}{n-1}X^\\top X\n=V\\frac{D^2}{n-1}V^\\top.\n$$\n\nPor tanto,\n\n$$\nA=V,\n\\qquad\n\\lambda_k=\\frac{d_k^2}{n-1},\n\\qquad\nZ=XV=UD.\n$$\n\n`prcomp()` usa esta descomposición SVD, que suele ser numéricamente preferible a calcular `eigen(cov(X))`.\n\nPara datos centrados,\n\n$$\n\\operatorname{rank}(X)\\leq\\min(n-1,p).\n$$\n\nEn particular, si $p>n$, puede haber como máximo $n-1$ componentes con varianza positiva.\n\n## PCA de `mtcars`\n\nUsaremos seis variables numéricas para conectar las fórmulas con un ejemplo pequeño. Como tienen unidades y dispersiones diferentes, el análisis principal se realiza sobre variables estandarizadas.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nX <- mtcars |>\n  select(mpg, disp, hp, drat, wt, qsec)\nXsc <- scale(X)\nS <- cov(Xsc)\neig <- eigen(S)\npca_mtcars <- prcomp(X, center = TRUE, scale. = TRUE)\n\nkable(summary(pca_mtcars)$importance, digits = 3)\n```\n\n::: {.cell-output-display}\n\n\n|                       |   PC1|   PC2|   PC3|   PC4|   PC5|   PC6|\n|:----------------------|-----:|-----:|-----:|-----:|-----:|-----:|\n|Standard deviation     | 2.046| 1.071| 0.577| 0.393| 0.353| 0.228|\n|Proportion of Variance | 0.698| 0.191| 0.056| 0.026| 0.021| 0.009|\n|Cumulative Proportion  | 0.698| 0.889| 0.945| 0.971| 0.991| 1.000|\n\n\n:::\n\n```{.r .cell-code}\nkable(round(pca_mtcars$rotation, 3), caption = \"Coeficientes de las componentes\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Coeficientes de las componentes\n\n|     |    PC1|    PC2|    PC3|    PC4|    PC5|    PC6|\n|:----|------:|------:|------:|------:|------:|------:|\n|mpg  | -0.459| -0.059|  0.195| -0.782|  0.111| -0.352|\n|disp |  0.466|  0.061| -0.097| -0.600| -0.295|  0.568|\n|hp   |  0.426| -0.361| -0.146| -0.123|  0.806| -0.048|\n|drat | -0.367| -0.437| -0.800| -0.023| -0.144|  0.113|\n|wt   |  0.439|  0.300| -0.418| -0.104| -0.230| -0.692|\n|qsec | -0.253|  0.763| -0.341| -0.043|  0.422|  0.242|\n\n\n:::\n:::\n\n\nLos elementos principales del resultado son:\n\n- `rotation`: autovectores o coeficientes usados para construir las componentes;\n- `x`: scores, es decir, coordenadas de los individuos;\n- `sdev`: desviaciones típicas de las componentes;\n- `sdev^2`: autovalores o varianzas de las componentes.\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntibble(\n  componente = colnames(pca_mtcars$x),\n  `Varianza de los scores` = apply(pca_mtcars$x, 2, var),\n  Autovalor = pca_mtcars$sdev^2\n) |>\n  mutate(across(where(is.numeric), ~ round(.x, 4))) |>\n  kable(caption = \"El autovalor es la varianza de la componente\")\n```\n\n::: {.cell-output-display}\n\n\nTable: El autovalor es la varianza de la componente\n\n|componente | Varianza de los scores| Autovalor|\n|:----------|----------------------:|---------:|\n|PC1        |                 4.1874|    4.1874|\n|PC2        |                 1.1481|    1.1481|\n|PC3        |                 0.3334|    0.3334|\n|PC4        |                 0.1544|    0.1544|\n|PC5        |                 0.1248|    0.1248|\n|PC6        |                 0.0520|    0.0520|\n\n\n:::\n:::\n\n\n## Scores y coeficientes\n\nSi $A$ es la matriz de autovectores, los scores son\n\n$$\nZ=XA.\n$$\n\nLos coeficientes $a_{jk}$ indican cómo entra la variable $X_j$ en la combinación lineal\n\n$$\nPC_k=a_{1k}X_1+\\cdots+a_{pk}X_p.\n$$\n\nEn cambio, los scores indican dónde queda cada individuo en los ejes nuevos.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nscores <- as_tibble(pca_mtcars$x, rownames = \"coche\")\n\nscores |>\n  select(coche, PC1, PC2) |>\n  slice_head(n = 6) |>\n  mutate(across(c(PC1, PC2), ~ round(.x, 2))) |>\n  kable(caption = \"Primeros scores\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Primeros scores\n\n|coche             |   PC1|   PC2|\n|:-----------------|-----:|-----:|\n|Mazda RX4         | -0.84| -0.87|\n|Mazda RX4 Wag     | -0.81| -0.56|\n|Datsun 710        | -1.69|  0.04|\n|Hornet 4 Drive    | -0.10|  1.29|\n|Hornet Sportabout |  1.29|  0.01|\n|Valiant           |  0.22|  2.01|\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nscores |>\n  mutate(transmision = factor(mtcars$am, labels = c(\"automático\", \"manual\"))) |>\n  ggplot(aes(PC1, PC2, color = transmision)) +\n  geom_hline(yintercept = 0, color = \"gray75\") +\n  geom_vline(xintercept = 0, color = \"gray75\") +\n  geom_point(size = 2, alpha = 0.85) +\n  labs(title = \"Individuos en el plano PCA\", color = \"Transmisión\") +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/mapa-individuos-1.png){width=672}\n:::\n:::\n\n\nPuntos cercanos tienen perfiles parecidos en las variables activas, con una cautela: si PC1--PC2 explica poca varianza, las proximidades de ese plano pueden ser engañosas.\n\nEl signo de una componente es arbitrario. Si $a_k$ es una solución, $-a_k$ también lo es; los scores cambian simultáneamente de $Xa_k$ a $-Xa_k$, sin alterar distancias, varianzas ni interpretación relativa. Por eso no conviene comparar signos entre programas sin tener en cuenta esta ambigüedad.\n\n## Coordenadas de las variables\n\nEl coeficiente de una variable y su correlación con una componente son cantidades distintas. En un PCA estandarizado, las coordenadas de las variables son\n\n$$\n\\operatorname{Cor}(X_j,PC_k)\n=a_{jk}\\sqrt{\\lambda_k}.\n$$\n\nComo `pca_mtcars$sdev[k]` es $\\sqrt{\\lambda_k}$, calculamos todas las coordenadas mediante:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncoord_var <- sweep(\n  pca_mtcars$rotation,\n  2,\n  pca_mtcars$sdev,\n  \"*\"\n)\ncoord_var_check <- cor(Xsc, pca_mtcars$x)\n\nstopifnot(isTRUE(all.equal(unname(coord_var), unname(coord_var_check))))\n```\n:::\n\n\n::: {.callout-note}\nEn distintos libros y paquetes, *loading* puede referirse a convenciones diferentes. Aquí distinguimos **coeficientes de la componente** (`rotation`) y **coordenadas de las variables** (correlaciones variable--componente).\n:::\n\n### Círculo de correlaciones\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncirculo <- tibble(\n  angulo = seq(0, 2 * pi, length.out = 361),\n  x = cos(angulo),\n  y = sin(angulo)\n)\ncoord_var_12 <- as_tibble(coord_var[, 1:2, drop = FALSE], rownames = \"variable\")\n\nggplot() +\n  geom_path(data = circulo, aes(x, y), color = \"gray55\") +\n  geom_hline(yintercept = 0, linetype = \"dashed\", color = \"gray70\") +\n  geom_vline(xintercept = 0, linetype = \"dashed\", color = \"gray70\") +\n  geom_segment(\n    data = coord_var_12,\n    aes(x = 0, y = 0, xend = PC1, yend = PC2),\n    arrow = grid::arrow(length = grid::unit(0.14, \"cm\")),\n    color = \"#2c7fb8\", linewidth = 0.8\n  ) +\n  geom_text(\n    data = coord_var_12,\n    aes(PC1, PC2, label = variable),\n    nudge_y = 0.045, size = 3.5\n  ) +\n  coord_equal(xlim = c(-1.08, 1.08), ylim = c(-1.08, 1.08)) +\n  labs(title = \"Círculo de correlaciones\", x = \"PC1\", y = \"PC2\") +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/circulo-correlaciones-1.png){width=672}\n:::\n:::\n\n\nFlechas próximas sugieren correlación positiva; flechas opuestas, correlación negativa; flechas aproximadamente perpendiculares, asociación lineal débil. Una longitud próxima a uno indica buena representación en PC1--PC2. La lectura angular es aproximada porque solo mostramos dos componentes.\n\n## Cos² y contribuciones\n\nEstas medidas responden a preguntas diferentes.\n\n| Cantidad | Fórmula en PCA estandarizado | Pregunta |\n|---|---|---|\n| Coeficiente | $a_{jk}$ | ¿Cómo entra $X_j$ en PC$_k$? |\n| Coordenada | $\\sqrt{\\lambda_k}a_{jk}$ | ¿Qué correlación tiene $X_j$ con PC$_k$? |\n| Cos² | $\\lambda_k a_{jk}^2$ | ¿Qué bien representa PC$_k$ a $X_j$? |\n| Contribución | $a_{jk}^2$ | ¿Cuánto participa $X_j$ en la construcción de PC$_k$? |\n\n### Cos² de las variables\n\nPara una variable estandarizada,\n\n$$\n\\cos^2_{jk}=\\operatorname{Cor}(X_j,PC_k)^2\n=\\lambda_k a_{jk}^2.\n$$\n\nPor eso no se obtiene elevando directamente `rotation` al cuadrado. La calidad en el plano PC1--PC2 es\n\n$$\n\\cos^2_j(PC1,PC2)\n=\\operatorname{Cor}(X_j,PC_1)^2\n+\\operatorname{Cor}(X_j,PC_2)^2.\n$$\n\nUn valor próximo a uno significa que casi toda la variabilidad de la variable es visible en el plano.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncos2_var <- coord_var^2\ncos2_var_plano <- rowSums(cos2_var[, 1:2, drop = FALSE])\n\nstopifnot(isTRUE(all.equal(\n  unname(rowSums(cos2_var)),\n  rep(1, nrow(cos2_var))\n)))\n\nas_tibble(cos2_var, rownames = \"variable\") |>\n  pivot_longer(-variable, names_to = \"componente\", values_to = \"cos2\") |>\n  ggplot(aes(componente, variable, fill = cos2)) +\n  geom_tile(color = \"white\") +\n  geom_text(aes(label = sprintf(\"%.2f\", cos2)), size = 3) +\n  labs(title = \"Cos² de las variables\", x = \"Componente\", y = NULL, fill = \"Cos²\") +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/cos2-variables-1.png){width=672}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntibble(\n  variable = names(cos2_var_plano),\n  cos2_plano = as.numeric(cos2_var_plano)\n) |>\n  ggplot(aes(cos2_plano, reorder(variable, cos2_plano))) +\n  geom_col(fill = \"#2c7fb8\", alpha = 0.85) +\n  scale_x_continuous(limits = c(0, 1)) +\n  labs(\n    title = \"Calidad de representación en el plano PC1-PC2\",\n    x = \"Cos² en el plano\", y = NULL\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/cos2-variables-plano-1.png){width=672}\n:::\n:::\n\n\n### Contribución de las variables\n\nLa contribución relativa de $X_j$ a PC$_k$ es\n\n$$\n\\operatorname{ctr}_{jk}\n=\\frac{\\operatorname{Cor}(X_j,PC_k)^2}{\\lambda_k}\n=a_{jk}^2.\n$$\n\nEn porcentaje es $100a_{jk}^2$. Como cada autovector tiene norma uno, las contribuciones a una componente suman 100%.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncontrib_var <- 100 * pca_mtcars$rotation^2\nstopifnot(isTRUE(all.equal(\n  unname(colSums(contrib_var)),\n  rep(100, ncol(contrib_var))\n)))\n\nas_tibble(contrib_var[, 1:2, drop = FALSE], rownames = \"variable\") |>\n  pivot_longer(-variable, names_to = \"componente\", values_to = \"contribucion\") |>\n  group_by(componente) |>\n  arrange(desc(contribucion), .by_group = TRUE) |>\n  ungroup() |>\n  mutate(contribucion = round(contribucion, 1)) |>\n  kable(caption = \"Contribución de las variables a PC1 y PC2 (%)\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Contribución de las variables a PC1 y PC2 (%)\n\n|variable |componente | contribucion|\n|:--------|:----------|------------:|\n|disp     |PC1        |         21.7|\n|mpg      |PC1        |         21.0|\n|wt       |PC1        |         19.2|\n|hp       |PC1        |         18.1|\n|drat     |PC1        |         13.5|\n|qsec     |PC1        |          6.4|\n|qsec     |PC2        |         58.2|\n|drat     |PC2        |         19.1|\n|hp       |PC2        |         13.1|\n|wt       |PC2        |          9.0|\n|disp     |PC2        |          0.4|\n|mpg      |PC2        |          0.3|\n\n\n:::\n:::\n\n\nEl cos² pregunta si una variable queda bien representada; la contribución pregunta cuánto ayudó a construir el eje. Una variable puede contribuir mucho a una componente sin que el plano completo represente bien toda su variabilidad.\n\n### Cos² de los individuos\n\nPara el individuo $i$, con scores $z_{ik}$,\n\n$$\nd_i^2=\\sum_{k=1}^p z_{ik}^2,\n\\qquad\n\\cos^2_{ik}=\\frac{z_{ik}^2}{d_i^2}.\n$$\n\nEn PC1--PC2,\n\n$$\n\\cos^2_i(PC1,PC2)\n=\\frac{z_{i1}^2+z_{i2}^2}{\\sum_{k=1}^p z_{ik}^2}.\n$$\n\nMide qué proporción de la distancia del individuo al centro queda visible en el plano.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nscores_all <- pca_mtcars$x\ndist2_ind <- rowSums(scores_all^2)\ncos2_ind <- sweep(scores_all^2, 1, dist2_ind, \"/\")\ncos2_ind_plano <- rowSums(cos2_ind[, 1:2, drop = FALSE])\n\nstopifnot(isTRUE(all.equal(\n  unname(rowSums(cos2_ind)),\n  rep(1, nrow(cos2_ind))\n)))\n\ntibble(\n  coche = rownames(scores_all),\n  PC1 = scores_all[, 1],\n  PC2 = scores_all[, 2],\n  cos2_plano = cos2_ind_plano,\n  transmision = factor(mtcars$am, labels = c(\"automático\", \"manual\"))\n) |>\n  ggplot(aes(PC1, PC2, color = transmision, size = cos2_plano)) +\n  geom_hline(yintercept = 0, color = \"gray80\") +\n  geom_vline(xintercept = 0, color = \"gray80\") +\n  geom_point(alpha = 0.8) +\n  scale_size_continuous(range = c(1.5, 5), limits = c(0, 1)) +\n  labs(\n    title = \"Calidad de representación de los individuos\",\n    subtitle = \"El tamaño representa el cos² en PC1-PC2\",\n    color = \"Transmisión\", size = \"Cos²\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/cos2-individuos-1.png){width=672}\n:::\n:::\n\n\nPara ver por qué cambia la calidad, comparamos automáticamente el individuo mejor representado con el peor representado en el plano.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ncalidad_individuos <- tibble(\n  coche = rownames(scores_all),\n  cos2_plano = cos2_ind_plano\n)\nindividuos_extremos <- bind_rows(\n  calidad_individuos |> slice_max(cos2_plano, n = 1, with_ties = FALSE),\n  calidad_individuos |> slice_min(cos2_plano, n = 1, with_ties = FALSE)\n) |>\n  mutate(calidad = c(\"Cos² alto\", \"Cos² bajo\"))\n\nas_tibble(scores_all, rownames = \"coche\") |>\n  semi_join(individuos_extremos, by = \"coche\") |>\n  pivot_longer(-coche, names_to = \"componente\", values_to = \"score\") |>\n  left_join(individuos_extremos, by = \"coche\") |>\n  ggplot(aes(componente, score, fill = calidad)) +\n  geom_col(show.legend = FALSE) +\n  facet_wrap(vars(coche, calidad), ncol = 1) +\n  labs(\n    title = \"Scores a través de todas las componentes\",\n    subtitle = \"En el segundo caso, componentes posteriores contienen una parte mayor de la distancia\",\n    x = NULL, y = \"Score\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/perfiles-cos2-individuos-1.png){width=672}\n:::\n:::\n\n\n## Biplot: dos geometrías en una figura\n\nUn mapa de individuos representa scores; un círculo de correlaciones representa correlaciones variable--componente. Un biplot los superpone, pero ambos objetos no están necesariamente en la misma escala numérica y la interpretación depende de la convención usada.\n\nEn el siguiente biplot exploratorio conservamos las direcciones del círculo de correlaciones y multiplicamos todas las flechas por un único factor para hacerlas visibles sobre el mapa de scores. Su longitud solo es interpretable de forma relativa; para leer correlaciones deben usarse las coordenadas del círculo anterior.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfactor_flechas <- 0.75 * max(sqrt(rowSums(pca_mtcars$x[, 1:2]^2)))\nbiplot_variables <- coord_var_12 |>\n  mutate(\n    PC1 = PC1 * factor_flechas,\n    PC2 = PC2 * factor_flechas\n  )\nbiplot_individuos <- scores |>\n  mutate(transmision = factor(mtcars$am, labels = c(\"automático\", \"manual\")))\n\nggplot(biplot_individuos, aes(PC1, PC2, color = transmision)) +\n  geom_point(alpha = 0.65) +\n  geom_segment(\n    data = biplot_variables,\n    aes(x = 0, y = 0, xend = PC1, yend = PC2),\n    inherit.aes = FALSE,\n    arrow = grid::arrow(length = grid::unit(0.14, \"cm\")),\n    color = \"black\"\n  ) +\n  geom_text(\n    data = biplot_variables,\n    aes(PC1, PC2, label = variable),\n    inherit.aes = FALSE, nudge_y = 0.18, size = 3.3\n  ) +\n  labs(\n    title = \"Biplot exploratorio de mtcars\",\n    subtitle = \"Las flechas se han reescalado por un único factor gráfico\",\n    color = \"Transmisión\"\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/biplot-mtcars-1.png){width=672}\n:::\n:::\n\n\n## Varianza explicada y número de componentes\n\n\n::: {.cell}\n\n```{.r .cell-code}\nvar_exp <- pca_mtcars$sdev^2 / sum(pca_mtcars$sdev^2)\nresumen_varianza <- tibble(\n  componente = factor(\n    paste0(\"PC\", seq_along(var_exp)),\n    levels = paste0(\"PC\", seq_along(var_exp))\n  ),\n  varianza = var_exp,\n  acumulada = cumsum(var_exp)\n)\n\nresumen_varianza |>\n  ggplot(aes(componente, varianza, group = 1)) +\n  geom_col(fill = \"#4c78a8\", alpha = 0.8) +\n  geom_line(color = \"#222222\") +\n  geom_point(size = 2) +\n  labs(title = \"Varianza explicada\", x = \"Componente\", y = \"Proporción de varianza\") +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/scree-plot-1.png){width=672}\n:::\n\n```{.r .cell-code}\nresumen_varianza |>\n  mutate(across(c(varianza, acumulada), ~ round(.x, 3))) |>\n  kable()\n```\n\n::: {.cell-output-display}\n\n\n|componente | varianza| acumulada|\n|:----------|--------:|---------:|\n|PC1        |    0.698|     0.698|\n|PC2        |    0.191|     0.889|\n|PC3        |    0.056|     0.945|\n|PC4        |    0.026|     0.971|\n|PC5        |    0.021|     0.991|\n|PC6        |    0.009|     1.000|\n\n\n:::\n:::\n\n\nNo existe un único número correcto de componentes con independencia del objetivo.\n\n| Objetivo | Criterio principal |\n|---|---|\n| Visualización | 2--3 componentes, informando de la varianza representada |\n| Resumen descriptivo | Varianza acumulada e interpretabilidad |\n| Compresión | Error de reconstrucción |\n| Eliminar redundancia | Autovalores pequeños y forma del *scree plot* |\n| Preprocesamiento predictivo | Rendimiento validado del modelo posterior |\n| Distinguir estructura de ruido | Criterios auxiliares como análisis paralelo |\n\nEn un PCA de la matriz de correlaciones, la regla de Kaiser conserva componentes con $\\lambda_k>1$: cada una explica más que una variable estandarizada promedio. Es una referencia, no una decisión automática.\n\nEl análisis paralelo compara los autovalores observados con los de datos aleatorios de las mismas dimensiones. Aquí usamos el percentil 95 de 500 simulaciones independientes.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(321)\nB <- 500\nautovalores_aleatorios <- replicate(\n  B,\n  eigen(\n    cor(matrix(rnorm(nrow(X) * ncol(X)), nrow = nrow(X), ncol = ncol(X))),\n    symmetric = TRUE,\n    only.values = TRUE\n  )$values\n)\nreferencia_p95 <- apply(autovalores_aleatorios, 1, quantile, probs = 0.95)\n\ntibble(\n  componente = factor(\n    paste0(\"PC\", seq_len(ncol(X))),\n    levels = paste0(\"PC\", seq_len(ncol(X)))\n  ),\n  Observado = pca_mtcars$sdev^2,\n  `Percentil 95 aleatorio` = referencia_p95\n) |>\n  pivot_longer(-componente, names_to = \"serie\", values_to = \"autovalor\") |>\n  ggplot(aes(componente, autovalor, color = serie, group = serie)) +\n  geom_line() +\n  geom_point(size = 2) +\n  labs(\n    title = \"Scree plot con referencia aleatoria\",\n    x = \"Componente\", y = \"Autovalor\", color = NULL\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/analisis-paralelo-1.png){width=672}\n:::\n:::\n\n\nEl análisis paralelo también es un criterio complementario: la decisión debe responder al uso posterior y al coste de perder información.\n\n## Covarianzas frente a correlaciones\n\nLa elección entre ambas geometrías no es automática. Usar covarianzas conserva las unidades originales; estandarizar equivale a usar correlaciones y da varianza inicial uno a cada variable.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nkable(round(cov(X), 2), caption = \"Matriz de covarianzas\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Matriz de covarianzas\n\n|     |     mpg|     disp|      hp|   drat|     wt|   qsec|\n|:----|-------:|--------:|-------:|------:|------:|------:|\n|mpg  |   36.32|  -633.10| -320.73|   2.20|  -5.12|   4.51|\n|disp | -633.10| 15360.80| 6721.16| -47.06| 107.68| -96.05|\n|hp   | -320.73|  6721.16| 4700.87| -16.45|  44.19| -86.77|\n|drat |    2.20|   -47.06|  -16.45|   0.29|  -0.37|   0.09|\n|wt   |   -5.12|   107.68|   44.19|  -0.37|   0.96|  -0.31|\n|qsec |    4.51|   -96.05|  -86.77|   0.09|  -0.31|   3.19|\n\n\n:::\n\n```{.r .cell-code}\nkable(round(cor(X), 2), caption = \"Matriz de correlaciones\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Matriz de correlaciones\n\n|     |   mpg|  disp|    hp|  drat|    wt|  qsec|\n|:----|-----:|-----:|-----:|-----:|-----:|-----:|\n|mpg  |  1.00| -0.85| -0.78|  0.68| -0.87|  0.42|\n|disp | -0.85|  1.00|  0.79| -0.71|  0.89| -0.43|\n|hp   | -0.78|  0.79|  1.00| -0.45|  0.66| -0.71|\n|drat |  0.68| -0.71| -0.45|  1.00| -0.71|  0.09|\n|wt   | -0.87|  0.89|  0.66| -0.71|  1.00| -0.17|\n|qsec |  0.42| -0.43| -0.71|  0.09| -0.17|  1.00|\n\n\n:::\n\n```{.r .cell-code}\npca_mtcars_cov <- prcomp(X, center = TRUE, scale. = FALSE)\npca_mtcars_cor <- prcomp(X, center = TRUE, scale. = TRUE)\n\ntibble(\n  componente = colnames(summary(pca_mtcars_cov)$importance),\n  covarianzas = round(summary(pca_mtcars_cov)$importance[2, ], 3),\n  correlaciones = round(summary(pca_mtcars_cor)$importance[2, ], 3)\n) |>\n  kable(caption = \"Proporción de varianza explicada\")\n```\n\n::: {.cell-output-display}\n\n\nTable: Proporción de varianza explicada\n\n|componente | covarianzas| correlaciones|\n|:----------|-----------:|-------------:|\n|PC1        |       0.927|         0.698|\n|PC2        |       0.072|         0.191|\n|PC3        |       0.000|         0.056|\n|PC4        |       0.000|         0.026|\n|PC5        |       0.000|         0.021|\n|PC6        |       0.000|         0.009|\n\n\n:::\n:::\n\n\nSin escalar, las variables con mayor dispersión en sus unidades originales tienen más influencia. Escalar modifica la pregunta: no es una corrección universal, sino una decisión sustantiva sobre qué diferencias deben contar.\n\n## Reducción de dimensión y reconstrucción\n\nSi retenemos $q<p$ componentes y definimos\n\n$$\nA_q=(a_1,\\ldots,a_q),\n\\qquad\nZ_q=XA_q,\n$$\n\nla reconstrucción en el espacio original centrado es\n\n$$\n\\boxed{\\hat X_q=Z_qA_q^\\top=XA_qA_q^\\top}.\n$$\n\nLa matriz $P_q=A_qA_q^\\top$ proyecta sobre el subespacio generado por las primeras $q$ componentes. Con la convención de covarianza que divide por $n-1$,\n\n$$\n\\|X-\\hat X_q\\|_F^2\n=(n-1)\\sum_{k=q+1}^{p}\\lambda_k.\n$$\n\nLa varianza descartada es exactamente el error cuadrático de reconstrucción. Además, entre todas las aproximaciones lineales de rango $q$, PCA minimiza ese error.\n\n\n::: {.cell}\n\n```{.r .cell-code}\np <- ncol(Xsc)\nlambda <- pca_mtcars$sdev^2\nerror_empirico <- sapply(seq_len(p), function(q) {\n  A_q <- pca_mtcars$rotation[, seq_len(q), drop = FALSE]\n  Z_q <- pca_mtcars$x[, seq_len(q), drop = FALSE]\n  X_aprox_q <- Z_q %*% t(A_q)\n  sum((Xsc - X_aprox_q)^2)\n})\nerror_teorico <- sapply(seq_len(p), function(q) {\n  varianza_descartada <- if (q < p) sum(lambda[seq.int(q + 1, p)]) else 0\n  (nrow(Xsc) - 1) * varianza_descartada\n})\n\ntibble(\n  componentes = seq_len(p),\n  error_empirico = error_empirico,\n  error_teorico = error_teorico\n) |>\n  pivot_longer(-componentes, names_to = \"calculo\", values_to = \"error\") |>\n  ggplot(aes(componentes, error, color = calculo)) +\n  geom_line() +\n  geom_point() +\n  scale_x_continuous(breaks = seq_len(p)) +\n  labs(\n    title = \"Error de reconstrucción al añadir componentes\",\n    x = \"Número de componentes retenidas\", y = \"Error cuadrático\", color = NULL\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/error-reconstruccion-1.png){width=672}\n:::\n\n```{.r .cell-code}\nstopifnot(isTRUE(all.equal(error_empirico, error_teorico)))\n```\n:::\n\n\nRetener componentes no elimina «variables poco importantes»: elimina direcciones de baja variabilidad. Elegir $q$ equivale a aceptar un error de reconstrucción concreto.\n\n## Máxima varianza no implica máxima relevancia\n\nPCA no conoce una variable respuesta ni nuestra pregunta científica. En el siguiente ejemplo, la mayor variabilidad está en la diagonal $x=y$, pero los grupos externos se distinguen en la dirección perpendicular, de mucha menor varianza.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(456)\nn_grupos <- 240\ngrupo <- factor(rep(c(\"Grupo A\", \"Grupo B\"), each = n_grupos / 2))\nu <- rnorm(n_grupos, sd = 3)\nv <- ifelse(grupo == \"Grupo A\", -0.45, 0.45) + rnorm(n_grupos, sd = 0.16)\ndatos_grupos <- tibble(\n  x = (u + v) / sqrt(2),\n  y = (u - v) / sqrt(2),\n  grupo = grupo\n)\npca_grupos <- prcomp(select(datos_grupos, x, y), center = TRUE, scale. = FALSE)\nvar_grupos <- 100 * pca_grupos$sdev^2 / sum(pca_grupos$sdev^2)\nejes_grupos <- tibble(\n  componente = paste0(\"PC\", 1:2),\n  xend = pca_grupos$rotation[1, ] * pca_grupos$sdev * 2,\n  yend = pca_grupos$rotation[2, ] * pca_grupos$sdev * 2\n)\n\ndatos_grupos |>\n  ggplot(aes(x, y, color = grupo)) +\n  geom_point(alpha = 0.65) +\n  geom_segment(\n    data = ejes_grupos,\n    aes(x = 0, y = 0, xend = xend, yend = yend),\n    inherit.aes = FALSE,\n    arrow = grid::arrow(length = grid::unit(0.16, \"cm\")),\n    color = \"black\", linewidth = 0.9\n  ) +\n  geom_text(\n    data = ejes_grupos,\n    aes(xend, yend, label = componente),\n    inherit.aes = FALSE, nudge_y = 0.18\n  ) +\n  coord_equal() +\n  labs(\n    title = \"La dirección más variable no es la que separa los grupos\",\n    subtitle = paste0(\n      \"PC1 explica \", round(var_grupos[1], 1),\n      \"% de la varianza; la separación externa está en PC2\"\n    ),\n    color = NULL\n  ) +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](06-pca_files/figure-html/varianza-frente-relevancia-1.png){width=672}\n:::\n:::\n\n\nUna dirección de baja varianza puede ser predictiva o científicamente decisiva. PCA preserva estructura geométrica dominante, no relevancia supervisada.\n\n## PCA como preprocesamiento\n\nPCA puede reducir ruido y colinealidad antes de clustering o clasificación. Por ejemplo, podemos agrupar los coches a partir de sus dos primeros scores:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(123)\ncl_pca <- kmeans(pca_mtcars$x[, 1:2], centers = 3, nstart = 25)\nkable(table(\n  cluster = cl_pca$cluster,\n  transmision = factor(mtcars$am, labels = c(\"automático\", \"manual\"))\n))\n```\n\n::: {.cell-output-display}\n\n\n| automático| manual|\n|----------:|------:|\n|          0|      6|\n|         12|      2|\n|          7|      5|\n\n\n:::\n:::\n\n\nLas componentes de mayor varianza no tienen por qué separar mejor una etiqueta externa como `am`.\n\nSi PCA forma parte de un procedimiento predictivo, hay que evitar fuga de información:\n\n1. dividir los datos según el esquema de validación;\n2. estimar centrado, escalado y PCA solo con el conjunto de entrenamiento;\n3. proyectar validación o test con esas transformaciones ya estimadas;\n4. elegir $q$ dentro de la validación si se optimiza según el rendimiento.\n\nAunque PCA no usa la respuesta, sí aprende la distribución de las covariables. Calcularlo sobre todo el conjunto antes de validar puede producir *leakage*.\n\n## Limitaciones\n\n- PCA es lineal y está pensado para variables numéricas.\n- Maximiza varianza, no separación de grupos ni capacidad predictiva.\n- Puede ser sensible a valores atípicos.\n- Las componentes pueden ser difíciles de interpretar.\n- Una dirección de baja varianza puede ser importante para la pregunta aplicada.\n- Los ejes cambian si cambian el escalado, las variables activas o la población.\n\n::: {.callout-warning}\nQue una componente explique mucha varianza no implica que sea causal, estable ni sustantivamente importante.\n:::\n\n## Errores frecuentes\n\n- Aplicar PCA sin centrar los datos.\n- Estandarizar de forma automática sin pensar qué geometría responde a la pregunta.\n- Interpretar PC1--PC2 sin revisar la varianza explicada y los cos².\n- Confundir los coeficientes de `rotation` con correlaciones variable--componente.\n- Calcular el cos² de variables como `rotation^2`; eso da contribuciones, no cos².\n- Interpretar las flechas reescaladas de un biplot como correlaciones exactas.\n- Creer que las componentes tienen una interpretación única y objetiva.\n- Usar PCA para separar clases olvidando que PCA no usa la etiqueta.\n- Estimar PCA antes de separar entrenamiento y test.\n- Interpretar el signo de una componente como si fuera absoluto.\n\n## Resumen del capítulo\n\nPCA proyecta una nube sobre ejes ortogonales que ordenan su variabilidad. Maximizar la varianza proyectada equivale a minimizar el error de reconstrucción, y la optimización conduce a los autovectores de la matriz de covarianzas o correlaciones. Los autovalores son las varianzas de los nuevos ejes; SVD permite obtenerlos directamente desde la matriz de datos.\n\nPara interpretar el resultado hay que distinguir coeficientes, scores, coordenadas de variables, cos² y contribuciones. Retener $q$ componentes significa proyectar sobre un subespacio de dimensión $q$ y aceptar la varianza descartada como error de reconstrucción. Esa compresión puede ser útil, pero la máxima varianza no garantiza máxima relevancia externa.\n\n## Ejercicios propuestos\n\n1. Aplica PCA a `mtcars` con escalado e interpreta PC1 y PC2 usando coeficientes, contribuciones y círculo de correlaciones.\n2. Compara PCA de `mtcars` con y sin escalado. Explica qué geometría responde a cada análisis.\n3. Verifica numéricamente que `var(pca$x[, k])` coincide con `pca$sdev[k]^2`.\n4. Simula una nube alargada en dos dimensiones y dibuja la proyección sobre una dirección distinta de PC1. Compara varianza y error.\n5. Comprueba que las correlaciones variable--componente coinciden con `sweep(rotation, 2, sdev, \"*\")`.\n6. Encuentra un coche bien representado en PC1--PC2 y otro mal representado. Compara sus scores en todas las componentes.\n7. Reconstruye `mtcars` con uno, dos y tres componentes y compara los errores con la suma de autovalores descartados.\n8. Explica por qué PCA no tiene por qué encontrar la dirección que mejor separa coches automáticos y manuales.\n9. Diseña correctamente un pipeline de validación en el que PCA preceda a un modelo predictivo.\n",
    "supporting": [
      "06-pca_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}